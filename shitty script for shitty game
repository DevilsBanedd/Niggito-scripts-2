local codes = {"FIRSTCODE", "20KADGIFT", "TYFOR30K", "50KDROPS"}
local Redeem = game:GetService("ReplicatedStorage").RemoteFunctions.MainRemoteFunction

for _, event in pairs(codes) do
    wait(0.5)
    Redeem:InvokeServer("RedeemPromoCode", event)
    end
